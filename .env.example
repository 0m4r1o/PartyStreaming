# Server port
PORT=3000

# Comma-separated allowed origins for CORS/WebSocket (e.g., your domain). Leave blank to allow same-origin.
ALLOWED_ORIGINS=

# Optional: Set a shared room admin PIN (numeric) for host controls
ADMIN_PIN=1234